# Full dependency list for model inference; this is not a model weights file.
# Step 1: install torch and torchvision for the CUDA version specified in README.md.
# Step 2: use the project Python to run -m pip install -r requirements-model.txt.
# Installed dependencies satisfying the version requirements are normally reused.

# Include the base dependencies (currently Pillow).
-r requirements.txt

# PyTorch: model computation; torchvision: companion vision utilities.
torch==2.6.0
torchvision==0.21.0

# Transformers: load Grounding DINO, preprocess inputs, and run inference.
transformers==4.51.3
