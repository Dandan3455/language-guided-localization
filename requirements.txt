# Base dependencies for the simulated example, visualization, and IoU evaluation.
# requirements-model.txt includes this file for real model inference.
# Pillow: open and process images, and draw bounding boxes.
Pillow==11.3.0
